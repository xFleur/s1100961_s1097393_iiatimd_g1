<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/playActivity">
    <activity
        android:id="@+id/playActivity"
        android:name="com.example.iiatimd_project_1920.MainActivity"
        android:label="activity_splash"
        tools:layout="@layout/activity_splash" />
    <activity
        android:id="@+id/quizActivity"
        android:name="com.example.iiatimd_project_1920.QuizActivity"
        android:label="activity_quiz"
        tools:layout="@layout/activity_quiz" />
    <action
        android:id="@+id/action_global_playActivity"
        app:destination="@id/playActivity" />
</navigation>
